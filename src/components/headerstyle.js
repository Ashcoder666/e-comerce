import {makeStyles} from "@material-ui/core/styles";

export default makeStyles(()=>({
    appbar:{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-between'
    },
    button:{
        marginRight:'20px'
    }
}));

